package com.example.demo.controller;

// Intentionally empty.
// The dashboard stats + announcement broadcast endpoints live in
// AdminDashboardController. Keep this file present so any existing references
// (and the AppRoutes lazy import on the frontend) don't break.
public class AdminController {
}
